export class Father {

  id: number;
  name: string;
  lastName: string;
  pesel: string;
  birthDate: Date;

  constructor(id: number, name: string, lastName: string, pesel: string, birthDate: Date) {
    this.id = id;
    this.name = name;
    this.lastName = lastName;
    this.pesel = pesel;
    this.birthDate = birthDate;
  }

}

